import { GlobalDataSummary } from './global-data-summary';

describe('GlobalDataSummary', () => {
  it('should create an instance', () => {
    expect(new GlobalDataSummary()).toBeTruthy();
  });
});
